import React from "react";
import Datos from "./Datos";
const Prioridad = () =>{
    return {
        <div>
          <><h3>{Datos.level}</h3><p>{Datos.description}</p><ul>
        {Datos.projects.maps}{projects.index} => {<li key={index}>{project}</li>}{"}"}&rbrace;
      </ul></>

        </div>
    };
};
export default Prioridad;